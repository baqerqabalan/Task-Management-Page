//Status enums
export enum StatusEnum {
  OPEN = 'Open',
  IN_PROGRESS = 'In Progress',
  EXCEEDS_DEADLINE = 'Exceeds Deadline',
  DONE = 'DONE',
}

//Priority Enums
export enum PriorityEnum {
  HIGH = 'High',
  MEDIUM = 'Medium',
  LOW = 'Low',
}
